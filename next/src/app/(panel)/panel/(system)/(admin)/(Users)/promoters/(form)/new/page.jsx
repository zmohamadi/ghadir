"use client";
import Form from '@/app/(panel)/panel/(system)/(admin)/(Users)/promoters/(form)/form';

export default function New(){
    return(
        <div>
            <Form />
        </div>
    );
}