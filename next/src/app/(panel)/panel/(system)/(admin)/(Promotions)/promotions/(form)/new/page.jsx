"use client";
import Form from '@/app/(panel)/panel/(system)/(admin)/(Promotions)/promotions/(form)/form';

export default function New(){
    return(
        <div>
            <Form />
        </div>
    );
}